import React from "react";
import GridDisplay from "../GridDisplay/GridDisplay";

const MainContainer = () => {
  return (
    <div>
      <GridDisplay />
    </div>
  );
};

export default MainContainer;
